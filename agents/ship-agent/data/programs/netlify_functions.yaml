# Netlify Functions Program Configuration
program_type: netlify_functions
vendor: highnote
version: "1.0.0"
api_type: graphql

metadata:
  description: "Netlify Functions Program - Highnote"
  base_url: "{{apiUrl}}"
  authentication:
    type: "bearer"
    header: "Authorization"

# Categories will be auto-populated from operations
categories: []

# Define your program-specific dimensions
dimensions:
  customer_segments: []
  program_variants: []
  features: []

# Define operation flows for common use cases
operation_flows:
  standard_flow:
    description: "Standard flow for netlify_functions"
    steps: []

tags:
  - netlify_functions
  - graphql
  - highnote
