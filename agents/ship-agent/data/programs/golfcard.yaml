# Golfcard Program Configuration
program_type: golfcard
vendor: highnote
version: "1.0.0"
api_type: graphql

metadata:
  description: "Golfcard Program - Highnote"
  base_url: "{{apiUrl}}"
  authentication:
    type: "bearer"
    header: "Authorization"

# Categories will be auto-populated from operations
categories: []

# Define your program-specific dimensions
dimensions:
  customer_segments: []
  program_variants: []
  features: []

# Define operation flows for common use cases
operation_flows:
  standard_flow:
    description: "Standard flow for golfcard"
    steps: []

tags:
  - golfcard
  - graphql
  - highnote
